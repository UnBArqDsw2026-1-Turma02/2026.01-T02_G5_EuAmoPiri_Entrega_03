export class SistemaTurismo {

    criarRelato(relato: string): void {
        console.log("Relato criado:");
        console.log(relato);
    }

    criarComentario(comentario: string): void {
        console.log("Comentário criado:");
        console.log(comentario);
    }

    cadastrarLocal(nome: string): void {
        console.log("Local cadastrado:");
        console.log(nome);
    }
}